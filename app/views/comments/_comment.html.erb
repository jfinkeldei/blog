<div class="comment">
  <p>
    <strong><%= User.find(comment.user_id).email %>:</strong> <%= comment.body %> <%= link_to 'Delete', [comment.article, comment],
                method: :delete,
                data: { confirm: "Are you sure?" } %>
  </p>
</div>
